<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.mdi.member.mapper.MemberMapper">

    <!-- 컬럼 ↔ DTO 매핑 -->
    <resultMap id="memberResultMap" type="kr.co.mdi.member.dto.MemberDTO">
        <id property="idMember" column="id_member"/>
        <result property="id" column="id"/>
        <result property="pass" column="pass"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="registDay" column="regist_day"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="failCount" column="fail_count"/>
        <result property="lastLogin" column="last_login"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="selectAllMembers" resultMap="memberResultMap">
        SELECT id_member, id, pass, name, email, regist_day, role, status,
               email_verified, fail_count, last_login, updated_at, deleted_at
        FROM member
    </select>

    <select id="selectMemberById" parameterType="long" resultMap="memberResultMap">
        SELECT id_member, id, pass, name, email, regist_day, role, status,
               email_verified, fail_count, last_login, updated_at, deleted_at
        FROM member
        WHERE id_member = #{id}
    </select>

    <insert id="insertMember" parameterType="kr.co.mdi.member.dto.MemberDTO" useGeneratedKeys="true" keyProperty="idMember">
        INSERT INTO member (id, pass, name, email, role, status, email_verified)
        VALUES (#{id}, #{pass}, #{name}, #{email}, #{role}, #{status}, #{emailVerified})
    </insert>

    <update id="updateMember" parameterType="kr.co.mdi.member.dto.MemberDTO">
        UPDATE member
        SET pass=#{pass}, name=#{name}, email=#{email}, role=#{role}, status=#{status},
            email_verified=#{emailVerified}, fail_count=#{failCount},
            last_login=#{lastLogin}, updated_at=#{updatedAt}, deleted_at=#{deletedAt}
        WHERE id_member=#{idMember}
    </update>

    <delete id="deleteMember" parameterType="long">
        DELETE FROM member WHERE id_member=#{id}
    </delete>

</mapper>
